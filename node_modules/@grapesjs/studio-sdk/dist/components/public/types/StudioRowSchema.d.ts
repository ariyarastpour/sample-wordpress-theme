import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutRowConfig extends StudioLayoutBaseConfig {
    /**
     * Type of the row.
     * @example "row"
     */
    type: `${StudioLayouts.row}`;
}
