import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutSidebarBottomConfig extends StudioLayoutBaseConfig {
    /**
     * Type of the sidebar bottom.
     * @example "sidebarBottom"
     */
    type: `${StudioLayouts.sidebarBottom}`;
}
