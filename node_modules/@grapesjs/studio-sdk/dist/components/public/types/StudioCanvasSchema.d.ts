import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutCanvasConfig extends StudioLayoutBaseConfig {
    /**
     * Type of the canvas.
     * @example "canvas"
     */
    type: `${StudioLayouts.canvas}`;
}
