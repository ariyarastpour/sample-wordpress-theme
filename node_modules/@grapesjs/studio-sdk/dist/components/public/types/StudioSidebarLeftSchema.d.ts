import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutSidebarLeftConfig extends Omit<StudioLayoutBaseConfig, 'htmlAttrs'> {
    /**
     * Type of the sidebar left.
     * @example "sidebarLeft"
     */
    type: `${StudioLayouts.sidebarLeft}`;
    /**
     * Indicates whether the sidebar is resizable.
     * @default false
     */
    resizable?: boolean;
}
