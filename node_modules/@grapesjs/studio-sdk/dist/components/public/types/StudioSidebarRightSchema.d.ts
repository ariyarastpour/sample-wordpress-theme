import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutSidebarRightConfig extends Omit<StudioLayoutBaseConfig, 'htmlAttrs'> {
    /**
     * Type of the sidebar right.
     * @example "sidebarRight"
     */
    type: `${StudioLayouts.sidebarRight}`;
}
