import { StudioLayouts } from '.';
import type { StudioLayoutPanelConfig } from './StudioPanelSchema';
export interface StudioLayoutPanelLayersConfig extends StudioLayoutPanelConfig {
    /**
     * Type of the panel layers.
     * @example "panelLayers"
     */
    type: `${StudioLayouts.panelLayers}`;
}
