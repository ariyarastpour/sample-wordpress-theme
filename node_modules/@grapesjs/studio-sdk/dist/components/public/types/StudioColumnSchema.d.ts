import { StudioLayouts } from '.';
import type { StudioLayoutBaseConfig } from './StudioBaseSchema';
export interface StudioLayoutColumnConfig extends StudioLayoutBaseConfig {
    /**
     * Type of the column.
     * @example "column"
     */
    type: `${StudioLayouts.column}`;
}
