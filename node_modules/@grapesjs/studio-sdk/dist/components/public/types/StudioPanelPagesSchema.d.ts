import { StudioLayouts } from '.';
import type { StudioLayoutPanelConfig } from './StudioPanelSchema';
export interface StudioLayoutPanelPagesConfig extends StudioLayoutPanelConfig {
    /**
     * Type of the panel page.
     * @example "panelPages"
     */
    type: `${StudioLayouts.panelPages}`;
}
